// Package env loads the service configuration from the environment.
package env

import (
	"fmt"
	"os"

	goenv "github.com/caarlos0/env/v11"
	"github.com/joho/godotenv"
	"go.uber.org/zap"
)

// Configuration mirrors env/.env.<APP_ENV>. Every field comes from the
// environment, with a default that keeps local development runnable.
type Configuration struct {
	AppEnv      string `env:"APP_ENV" envDefault:"local"`
	LogLevel    string `env:"LOG_LEVEL" envDefault:"debug"`
	RunningMode string `env:"RUNNING_MODE" envDefault:"main"` // fallback or main
	HTTPAddr    string `env:"HTTP_ADDR" envDefault:"0.0.0.0:8080"`
	APISecret   string `env:"API_SECRET"`

	DBHost     string `env:"DB_HOST"`
	DBPort     string `env:"DB_PORT" envDefault:"3306"`
	DBName     string `env:"DB_NAME"`
	DBUsername string `env:"DB_USERNAME"`
	DBPassword string `env:"DB_PASSWORD"`
	DBIdleConn int    `env:"DB_IDLE_CONN" envDefault:"10"`
	DBMaxConn  int    `env:"DB_MAX_CONN" envDefault:"100"`
}

// GetEnvConfig loads env/.env.<APP_ENV>, when present, then parses the
// environment into a Configuration.
func GetEnvConfig(log *zap.SugaredLogger) (*Configuration, error) {
	loadDotEnv(log)

	cfg := Configuration{}
	if err := goenv.Parse(&cfg); err != nil {
		return nil, fmt.Errorf("parse environment: %w", err)
	}

	log.Infow("configuration loaded",
		"app_env", cfg.AppEnv,
		"running_mode", cfg.RunningMode,
		"http_addr", cfg.HTTPAddr,
	)
	return &cfg, nil
}

// loadDotEnv reads the dotenv file matching APP_ENV. A missing file is not an
// error: in production the values come from the environment itself.
func loadDotEnv(log *zap.SugaredLogger) {
	appEnv := os.Getenv("APP_ENV")

	switch appEnv {
	case "local", "dev", "staging", "prod":
		path := fmt.Sprintf("./env/.env.%s", appEnv)
		if err := godotenv.Load(path); err != nil {
			log.Debugw("no dotenv file, reading the environment", "path", path)
		}
	default:
		if err := godotenv.Load(); err != nil {
			log.Debugw("no .env file, reading the environment", "app_env", appEnv)
		}
	}
}
